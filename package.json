{
  "name": "agentic-underwriting",
  "version": "0.1.0",
  "private": true,
  "description": "Local chatbot wrapper for an agentic underwriting platform.",
  "scripts": {
    "dev": ".venv/bin/python -m backend.main",
    "start": ".venv/bin/python -m backend.main",
    "mcp": ".venv/bin/python -m backend.mcp_server"
  }
}
